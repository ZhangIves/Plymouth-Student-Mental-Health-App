<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loong.x.app.dao.SysImageDao">

    <resultMap type="com.loong.x.app.entity.SysImage" id="SysImageMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="repairId" column="repair_id" jdbcType="VARCHAR"/>
        <result property="complaintId" column="complaint_id" jdbcType="VARCHAR"/>
        <result property="imageUrl" column="image_url" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SysImageMap">
        select
          id, repair_id, complaint_id, image_url
        from sys_image
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SysImageMap">
        select
          id, repair_id, complaint_id, image_url
        from sys_image
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="repairId != null and repairId != ''">
                and repair_id = #{repairId}
            </if>
            <if test="complaintId != null and complaintId != ''">
                and complaint_id = #{complaintId}
            </if>
            <if test="imageUrl != null and imageUrl != ''">
                and image_url = #{imageUrl}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sys_image
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="repairId != null and repairId != ''">
                and repair_id = #{repairId}
            </if>
            <if test="complaintId != null and complaintId != ''">
                and complaint_id = #{complaintId}
            </if>
            <if test="imageUrl != null and imageUrl != ''">
                and image_url = #{imageUrl}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sys_image(repair_id, complaint_id, image_url)
        values (#{repairId}, #{complaintId}, #{imageUrl})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_image(repair_id, complaint_id, image_url)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.repairId}, #{entity.complaintId}, #{entity.imageUrl})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_image(repair_id, complaint_id, image_url)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.repairId}, #{entity.complaintId}, #{entity.imageUrl})
        </foreach>
        on duplicate key update
        repair_id = values(repair_id),
        complaint_id = values(complaint_id),
        image_url = values(image_url)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sys_image
        <set>
            <if test="repairId != null and repairId != ''">
                repair_id = #{repairId},
            </if>
            <if test="complaintId != null and complaintId != ''">
                complaint_id = #{complaintId},
            </if>
            <if test="imageUrl != null and imageUrl != ''">
                image_url = #{imageUrl},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sys_image where id = #{id}
    </delete>

</mapper>

