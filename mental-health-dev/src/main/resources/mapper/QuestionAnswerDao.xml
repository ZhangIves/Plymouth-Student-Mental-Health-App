<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loong.x.app.dao.QuestionAnswerDao">

    <resultMap type="com.loong.x.app.entity.QuestionAnswer" id="QuestionAnswerMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="evaluationId" column="evaluation_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="QuestionAnswerMap">
        select
          id, evaluation_id, create_time, update_time, remark
        from question_answer
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="QuestionAnswerMap">
        select
          id, evaluation_id, create_time, update_time, remark
        from question_answer
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="evaluationId != null">
                and evaluation_id = #{evaluationId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="remark != null and remark != ''">
                and remark = #{remark}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from question_answer
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="evaluationId != null">
                and evaluation_id = #{evaluationId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="remark != null and remark != ''">
                and remark = #{remark}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into question_answer(evaluation_id, create_time, update_time, remark)
        values (#{evaluationId}, #{createTime}, #{updateTime}, #{remark})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into question_answer(evaluation_id, create_time, update_time, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.evaluationId}, #{entity.createTime}, #{entity.updateTime}, #{entity.remark})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into question_answer(evaluation_id, create_time, update_time, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.evaluationId}, #{entity.createTime}, #{entity.updateTime}, #{entity.remark})
        </foreach>
        on duplicate key update
        evaluation_id = values(evaluation_id),
        create_time = values(create_time),
        update_time = values(update_time),
        remark = values(remark)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update question_answer
        <set>
            <if test="evaluationId != null">
                evaluation_id = #{evaluationId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from question_answer where id = #{id}
    </delete>

</mapper>

