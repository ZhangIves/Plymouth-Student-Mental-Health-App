<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loong.x.app.dao.SysBannerDao">

    <resultMap type="com.loong.x.app.entity.SysBanner" id="SysBannerMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="img" column="img" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="sortNum" column="sort_num" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SysBannerMap">
        select
          id, img, title, status, sort_num, create_time, update_time
        from sys_banner
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SysBannerMap">
        select
          id, img, title, status, sort_num, create_time, update_time
        from sys_banner
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="img != null and img != ''">
                and img = #{img}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="sortNum != null">
                and sort_num = #{sortNum}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sys_banner
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="img != null and img != ''">
                and img = #{img}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="sortNum != null">
                and sort_num = #{sortNum}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sys_banner(img, title, status, sort_num, create_time, update_time)
        values (#{img}, #{title}, #{status}, #{sortNum}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_banner(img, title, status, sort_num, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.img}, #{entity.title}, #{entity.status}, #{entity.sortNum}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_banner(img, title, status, sort_num, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.img}, #{entity.title}, #{entity.status}, #{entity.sortNum}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        img = values(img),
        title = values(title),
        status = values(status),
        sort_num = values(sort_num),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sys_banner
        <set>
            <if test="img != null and img != ''">
                img = #{img},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="sortNum != null">
                sort_num = #{sortNum},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sys_banner where id = #{id}
    </delete>

</mapper>

